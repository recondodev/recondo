/**
 * Catalog parity lint — Phase 1 placeholder.
 *
 * Replaced in C11 with the real implementation that asserts every MCP
 * tool maps 1:1 to a `@recondo/data` export name (with an explicit
 * opt-out set documented inline). For C1, this is a no-op stub so the
 * `just mcp-lint-parity` recipe can be wired into the justfile without
 * a missing-file error.
 */

console.error("[mcp-lint-parity] phase 1 — placeholder until C11");
process.exit(0);
